import 'package:flutter/material.dart';

const colorBlack = Color(0xFF302F30);
const colorBlue = Color(0xff6279d9);
const colorGrey = Color(0xFFDADADA);
const colorWhite = Colors.white;

Widget verticalSpace(double height) {
  return SizedBox(
    height: height,
  );
}

Widget horizontalSpace(double width) {
  return SizedBox(
    width: width,
  );
}
